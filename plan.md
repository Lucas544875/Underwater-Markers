# 『老人と海｜潮の記憶』実装仕様

## 体験

- 画像を使わず、青空文庫の石波杏訳『老人と海』全文を読む単一ページ。
- 青と褪せた紙色、水平線、夕陽をCSSだけで構成したノスタルジックな装丁。
- カーソル移動とスクロールを流体場への速度入力として扱い、表示中の文節が流れから力を受けて漂う。
- 読書位置を上部バーの割合と「岸辺／出帆／沖へ／大魚／帰路／陸の灯／余白」で表示する。
- ユーザー操作で潮流を停止でき、OSの視差効果を減らす設定にも対応する。

## 流体と負荷対策

- Canvas 2D上に42px間隔の画面座標グリッドを確保し、拡散・移流・減衰を約32fpsで計算する。
- `Intl.Segmenter` で日本語を短い文節近似へ分割し、各DOM文節に速度・質量・位置を持たせる。
- `IntersectionObserver` により画面から180px以内に近づいた段落だけを遅延分節化する。
- 物理更新時にも個々の文節座標を判定し、画面外の要素を計算から除外する。
- 画面外の段落は更新集合から外し、復帰時にだけ基準位置を再計測する。
- 流体グリッドのdevicePixelRatioは1.5を上限にする。

## 本文と表示

- 原文のルビ構造を保持する。
- 青空文庫の改ページ注記は装飾区切りへ変換する。
- 訳者謝辞と参考文献一覧も収録する。
- 公式作品ファイル、図書カード、CC BY 2.1 JPへのリンクを末尾に表示する。
- 段落化・分節化・動的配置を行ったこと、翻訳内容を変更していないことを明記する。
