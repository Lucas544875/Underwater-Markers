# 『水底の標識』演出再設計版 実装計画

## 1. 現在の実装範囲

`plan/story.md` の物語とニュースサイト形式を維持し、`plan/horror-direction.md` に基づく記録劣化演出を独立レイヤーとして実装する。

- 「速報」から「削除済み原記事」まで全7記事を収録する。
- 関連記事リンクと記録一覧から記事を移動できる。
- 既読記事、最終記録片の確認状態を `localStorage` に保存する。
- 記録初期化ダイアログから保存状態を消去できる。
- 最終記事の4枚の記録片はドラッグ・キーボード操作に対応する。
- 最終記録片を一定数確認するとラストメッセージを表示する。
- デスクトップ、モバイル、キーボード操作へ対応する。

## 2. 技術構成

- HTML5 / CSS3 / Vanilla JavaScript (ES Modules)
- Vite: 開発サーバーと静的ビルド
- `localStorage`: `underwater-markers:v1` キーで閲覧状態を保存
- Playwright: デスクトップ・モバイルの実動作検証
- Intl.Segmenter: 本文を文節近似で分割し、粒子として波に応答させる
- Canvas 2D: 赤い染料と下端の濁り

### モジュール

- `src/content.js`: 記事、資料画像、最終記録片の宣言データ
- `src/state.js`: 既読履歴と最終記録状態の永続化
- `src/app.js`: 記事表示、画面遷移、記録一覧、最終記録片の操作
- `src/effects.js`: 文節分割、粒子シミュレーション、波入力、染料・濁りCanvas、損傷度
- `src/styles.css`: ニュース紙面、記録一覧、最終アーカイブ、レスポンシブ表示

## 3. 再設計した演出

- WebGLと浮遊オブジェクト、Stable Fluids系の速度場は使用しない。
- 本文を文節単位の粒子に分割し、操作が生む波で本来の位置から押し流す。深度4以降は流された文節が薄れたまま戻り切らない。
- 写真と最終記録片は演出を消去する固定境界として扱う。
- 赤い目印の複製は写真フレーム内だけに固定表示する。
- 深度に応じてDOM文字、撮影日時、記者欄、キャプションを段階的に欠落させる。
- 第3記事以降、累積損傷に応じて画面下端から黒い濁りを上げる。
- 損傷度と操作量を既読履歴とともに保存する。

## 4. 実装タスク

- [x] ホラー演出の採用・不採用基準と深度別進行を設計する。
- [x] 浮遊する標識、画像、検索語を使用しない構成にする。
- [x] 文節分割(Intl.Segmenter+助詞結合)と粒子化を実装する。
- [x] スクロール・ポインターの波による押し流しと損傷保存を実装する。
- [x] 写真・記録片を演出が侵入しない固定境界にする。
- [x] 写真内の赤い複製とメタデータ欠落を実装する。
- [x] 深度1、4、6、7とモバイル表示をブラウザで検証する。
- [x] 本番ビルドと構文検査を完了する。

## 5. 完了条件

- 第1記事は通常のニュース記事として判読でき、深層では文節が波に押し流されて欠けていく。
- 画面上を独立して浮遊する標識、画像、検索語が存在しない。
- 写真は歪まず、赤い複製と照合表示がフレーム内だけに存在する。
- WebGLコードを使用せず、Canvas演出が黒いタイルを発生させない。
- 再読み込み後も既読状態が残り、初期化で消去できる。
- 最終記録片を操作してラストメッセージを表示できる。
- 320px以上の画面幅で横スクロールが発生しない。
- `npm run build` とブラウザ検証が成功する。
