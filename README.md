# 水底の標識

地方ニュースサイトの形式で全7記事を読む、インタラクティブ・ミステリー作品です。既読状態はブラウザに保存されます。

```bash
npm install
npm run dev
```

本番用の静的ファイルは `npm run build` で `dist/` に生成されます。

企画書は `plan/story.md`、確定仕様と実装タスクは `plan.md` を参照してください。
